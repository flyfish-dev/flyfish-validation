<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>dev.flyfish</groupId>
        <artifactId>flyfish-validation-parent</artifactId>
        <version>1.0.0</version>
    </parent>
    <artifactId>flyfish-validation-spring-boot2-starter</artifactId>
    <packaging>jar</packaging>
    <name>Flyfish Validation Spring Boot 2 Starter</name>
    <description>Spring Boot 2 开箱即用依赖入口</description>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot2.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>dev.flyfish</groupId>
            <artifactId>flyfish-validation-spring-boot2-autoconfigure</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
    </dependencies>
    <properties>
        <maven.compiler.release>8</maven.compiler.release>
        <automatic.module.name>dev.flyfish.validation.boot2.starter</automatic.module.name>
    </properties>
</project>
